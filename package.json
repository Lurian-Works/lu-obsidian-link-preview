{
  "name": "lu-obsidian-link-preview",
  "version": "1.0.0",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "release:zip": "npm run build && node scripts/make-release.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "typescript": "^5.0.0"
  },
  "dependencies": {

  }
}
